<p id="notice"><%= notice %></p>

<p>
  <b>Album Name:</b>
  <%= @album.name %>
</p>

<%= link_to 'Edit this Photo', edit_album_photo_path(@album, @photo) %> |
<%= link_to 'Back to All Photos for this Album', album_path(@album) %>

<!-- Need to update this so that the size is manageable -->
<p>
  <b>Photo Name:</b>
    <%= @photo.name %> </br>
    <p id="individual_image">
      <%= image_tag(@photo.picture) %>
    </p>
</p>


<h5>Comments</h5>
<%= render "comments/comments" %>
<%= render "comments/form" %>